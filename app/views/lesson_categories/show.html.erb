<p style="color: green"><%= notice %></p>

<%= render @lesson_category %>

<div>
  <%= link_to "Edit this lesson category", edit_lesson_category_path(@lesson_category) %> |
  <%= link_to "Back to lesson categories", lesson_categories_path %>

  <%= button_to "Destroy this lesson category", @lesson_category, method: :delete, class: 'btn btn-danger' %>
</div>
